node_modules
dist
dist-ssr
*.local
.DS_Store

# Scraped card data (generated by the scrapers). The scraper TOOLS are
# committed; only their generated output is ignored.
/scrape/

# Card art moved into the app by `npm run build:assets` (~510 MB). The card
# DATA under public/cards/ IS committed (the catalog); only the heavy images
# are ignored — regenerate them by re-running the scrape + build:assets.
/public/card-images/

.vercel
.env*
