node_modules
dist
dist-ssr
*.local
.DS_Store

# Scraped OPTCG card data (generated by `npm run scrape`).
# The scraper TOOL lives in scripts/scrape/ and IS committed; only its
# generated output directory is ignored — it can be regenerated any time and
# would otherwise be thousands of large JSON files.
/scrape/
