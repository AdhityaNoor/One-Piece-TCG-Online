/**
 * (Empty.) Previously held the ACTIVATE_COUNTER_EVENT rejection stub. That
 * action is now implemented — see ./activateCounterEvent.ts. Kept as a
 * placeholder module to avoid a dangling import; safe to delete.
 */
export {};
